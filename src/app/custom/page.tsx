import { CustomPageClient } from "@/components/custom/custom-page-client";

export const metadata = {
  title: "Custom Embroidery",
  description:
    "Commission bespoke embroidery for your brand, team, or wardrobe. Premium quote experience.",
};

export default function CustomPage() {
  return <CustomPageClient />;
}
